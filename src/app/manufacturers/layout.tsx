// Scopes manufacturer.css to /manufacturers* routes only.
import "./manufacturer.css";

export default function ManufacturersLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
