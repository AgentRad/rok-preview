.DS_Store
Thumbs.db
node_modules/
.env
.env.local
.env*.local
.vercel
.claude/
.next/
next-env.d.ts
*.tsbuildinfo
.env*
test-runs/

# Testing team artifacts that spill into repo root (probe scripts,
# screenshots, result JSON, output dumps). Belt-and-suspenders alongside
# the test-runs/ ignore in case the testers run scripts in repo root.
/anon-*
/buyer-*
/supplier-*
/admin-*
/oem-*
/*-test.mjs
/*-test-result.json
/*-probe.mjs
/*-result.json
/*-out.txt
/*-out[0-9].txt
# Curl cookie jars (used during local API smoke tests). MUST NOT be committed:
# they contain session JWTs that, if leaked, let anyone impersonate the user.
/*.cookie-jar
/jar.txt
/cookies.txt
.local-secrets.env
